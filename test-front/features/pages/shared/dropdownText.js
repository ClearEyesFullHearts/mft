const { By } = require('selenium-webdriver');

class DropDownTextComponent {

    constructor(baseID) {
        // this.label = By.id(`${baseID}-label`);
        // this.text = By.id(`${baseID}-text`);
        // this.validate = By.id(`${baseID}-ok`);
        // this.cancel = By.id(`${baseID}-ko`);
    }
}

module.exports = DropDownTextComponent;